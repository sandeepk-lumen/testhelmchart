apiVersion: v1
kind: Service
metadata:
  name: {{ include "newchartforgitdemo.fullname" . }}
  labels:
    {{- include "newchartforgitdemo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "newchartforgitdemo.selectorLabels" . | nindent 4 }}
