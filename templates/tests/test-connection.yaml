apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "newchartforgitdemo.fullname" . }}-test-connection"
  labels:
    {{- include "newchartforgitdemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "newchartforgitdemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
