apiVersion: v1
kind: Service
metadata:
  name: {{ include "test2demo.fullname" . }}
  labels:
    {{- include "test2demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test2demo.selectorLabels" . | nindent 4 }}
