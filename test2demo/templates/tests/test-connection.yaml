apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test2demo.fullname" . }}-test-connection"
  labels:
    {{- include "test2demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test2demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
